/**
 * CardCollection constructor
 *
 * We simply keep track of an array of cards within this collection
 */
var CardCollection = function() {
	this.cards = []; // Array of Card instances
};

module.exports = CardCollection;